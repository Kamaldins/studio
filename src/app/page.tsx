
// This page is no longer used, the new main page is at src/app/[lang]/page.tsx
// We redirect to the default locale from the middleware.
// This file can be deleted, but we'll keep it for now.
export default function Home() {
  return null;
}
